import type { AssembledOptions } from './types';
export declare const buildOptions: (projectOptions: AssembledOptions) => AssembledOptions;
export declare const getConfiguration: (rootDirectory: string) => AssembledOptions;
