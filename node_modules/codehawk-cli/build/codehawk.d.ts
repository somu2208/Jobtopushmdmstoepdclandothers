import { calculateComplexity } from './analyze';
import type { AssembledOptions, FullyAnalyzedEntity, FullyAnalyzedFile, ResultsSummary } from './types';
import { generateBadge } from './badge';
export interface Results {
    options: AssembledOptions;
    resultsList: FullyAnalyzedFile[];
    fullResultsTree: FullyAnalyzedEntity[];
    summary: ResultsSummary;
}
declare const analyzeProject: (rawPath: string, isCliContext?: boolean) => Results;
export { calculateComplexity, analyzeProject, generateBadge };
