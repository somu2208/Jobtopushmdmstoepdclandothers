import * as fs from 'fs';
import type { AssembledOptions } from '../types';
export declare const getFsEntity: (fullPath: string) => fs.Stats | null;
export declare const shouldSeeEntity: ({ dir, entity, filename, fullPath, options, relativeDir, }: {
    dir: string;
    entity: fs.Stats;
    filename: string;
    fullPath: string;
    options: AssembledOptions;
    relativeDir: string;
}) => boolean;
export declare const shouldAnalyzeEntity: ({ entity, filename, fullPath, options, relativeDir, }: {
    entity: fs.Stats;
    filename: string;
    fullPath: string;
    options: AssembledOptions;
    relativeDir: string;
}) => boolean;
